1. CREATE SSH KEY
ssh-keygen -t ed25519 -C "your_email@example.com"

2.ADD SSH KEY TO GITHUB 
print your public SSH key and copy it
cat ~/.ssh/id_rsa.pub
in GitHub, navigate to Settings > SSH and GPG keys, and add a new SSH key. Paste your key in and save it.

3. CLONE YOUR REPOSITORY USING SSH
git clone git@github.com:your-username/42-piscine-backup.git

4. TEST YOUR CONNECTION
To verify that SSH is set up correctly, run:
ssh -T git@github.com
